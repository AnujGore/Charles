#Using Bert to detect sentiment in text

#First we will install tensorflow and do a rough sentiment analysis via IMDb movie reviews to enure that the 
#installation and operation is correct.

import os
import shutil

import tensorflow as tf
import tensorflow_hub as hub
import tensorflow_text as text

##Need to resolve tf-models issue. For now, using google notebooks






